# Dependencias de youtube-transcripts
# Instalar en venv local: python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
# Python 3.10.7 — ver docs/TAREAS_YOUTUBE.md seccion 7.

youtube-transcript-api==1.2.4
yt-dlp==2026.8.19
faster-whisper==1.2.1
mcp
pytest
pytest-asyncio
ruff==0.16.9  # linter pre-commit (ver .ai/commands.md variante B paso 2)
